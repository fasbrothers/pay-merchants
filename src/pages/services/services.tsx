import { AddTitle } from '../../components/add-title';

export default function Services() {
	return (
		<div>
			<div>
				<AddTitle url='add-service' title='Create a service' />
			</div>
		</div>
	);
}
